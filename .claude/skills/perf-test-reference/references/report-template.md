# 결과 보고서 템플릿

아래 템플릿을 매 라운드마다 작성한다.

````markdown
# Round {N}: {라운드 제목}

## 변경 사항

| 항목 | Before | After | 변경 이유 |
|------|--------|-------|----------|
| {설정명} | {이전 값} | {변경 값} | {왜 이 값으로 바꿨는지} |

**변경한 것**: {정확히 무엇을 바꿨는지 1줄 요약}
**변경하지 않은 것**: {나머지 조건이 동일함을 명시}

## 테스트 조건

| 항목 | 값 |
|------|-----|
| k6 스크립트 | `scenarios/load-test.js` (커밋: {hash}) |
| 부하 프로파일 | write {N} req/s, read {N} req/s, {M}분 |
| 초기 데이터 | TRUNCATE + FLUSH 완료 |
| JVM warm-up | dry-run 30초 완료 |
| 테스트 시각 | {YYYY-MM-DD HH:MM} ~ {HH:MM} |

## 결과

### k6 Summary

| 지표 | Before (Round {N-1}) | After (Round {N}) | 변화 | SLO | 판정 |
|------|---------------------|-------------------|------|-----|------|
| p95 응답시간 | {X}ms | {Y}ms | {비율} | ≤200ms | PASS/FAIL |
| p99 응답시간 | {X}ms | {Y}ms | {비율} | ≤500ms | PASS/FAIL |
| 에러율 | {X}% | {Y}% | | ≤0.1% | PASS/FAIL |
| TPS (실측) | {X} | {Y} | | ≥{목표} | PASS/FAIL |

### 에러 상세 (에러가 있을 경우)

| HTTP Status | 발생 수 | 비율 | 영향 받은 엔드포인트 | 원인 |
|-------------|---------|------|---------------------|------|
| {status} | {count} | {%} | {endpoint} | {원인} |

### 내부 지표 (Grafana)

| 지표 | Before | After | 해석 |
|------|--------|-------|------|
| HikariCP Pending max | | | |
| Tomcat busy threads max | | | |
| Consumer lag (peak) | | | |
| Consumer 처리량 (msg/s) | | | |
| Redis latency (avg) | | | |

### Grafana 캡처

> 캡처 시간 범위: {시작} ~ {종료} (k6 실행 시간과 동일)

#### {지표 이름}
![{설명}]({이미지 경로})

## 분석

### 왜 이런 결과가 나왔는가
{원인 분석 — 추측이 아닌 데이터 기반}

### 기대와 다른 점
{예상했던 것 vs 실제 — 있을 경우}

### 논리 완결성 체크 (작성 전 확인)

| 항목 | 확인 |
|------|------|
| 이론값(평균 기반) 사용 시 → "이론 상한" 명시 + 실제 한계가 낮은 이유 1줄 추가 | ☐ |
| 자원 투입 대비 효과 gap 발생 시(e.g. 5배 투입 → 3배 결과) → 병목 원인 특정 | ☐ |
| 수치 등장 시(실패율, 지연율 등) → 출처 명시 (설정값인지 실측값인지) | ☐ |
| 특정 옵션 기각 시 → 독자가 "그러면 X는?" 하고 물을 수 있는 대안도 선제적으로 차단 | ☐ |

## 다음 라운드 계획

| 변경 예정 | 이유 | 예상 효과 |
|----------|------|----------|
| {항목} | {왜} | {기대치} |
````

## 개선 효과 판정 기준

| 비교 항목 | 유효한 비교 | 무효한 비교 |
|----------|------------|------------|
| 동일 k6 스크립트 | ✅ | |
| 동일 데이터 초기 상태 | ✅ | |
| 1개 변수만 변경 | ✅ | |
| k6 스크립트 변경 + 설정 변경 | | ❌ before/after 비교 불가 |
| 2개 이상 동시 변경 | | ❌ 기여도 분리 불가 |

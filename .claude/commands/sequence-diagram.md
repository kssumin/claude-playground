---
allowed-tools: Read, Glob, Grep, Bash, Write, Edit
---

# /sequence-diagram

`sequence-diagram` 스킬을 사용해 `docs/sequence-diagram.md`를 생성/업데이트한다.

## Usage

```
/sequence-diagram              # 현재 브랜치 기준으로 전체 다이어그램 생성
/sequence-diagram feature3     # 특정 브랜치 기준으로 생성
```
